<%= form_for :recovery, :url => recoveries_path do |f| %>
  <%= f.text_field :email, :placeholder => 'e-mail',
                           :class => 'recovery_email' %>
  <%= f.submit 'Recovery', :class => 'recovery_submit' %>
<% end %>

